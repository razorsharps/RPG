#ifndef GAME_H
#define GAME_H

#include <GL/glew.h>
#include <GL/glfw.h>
#include <glm/glm.hpp>
#include <vector>
#include <algorithm>

class Game {
	private:
		
	public:
		Game();
		virtual ~Game();
};

#endif