#pragma once

#include <iostream>
#include "Observer.h"

class KeyObserver: public Observer {
  public:
   /* KeyObserver(Subject *mod): Observer(mod){}

    void update() {
        std::cout << "Herpaderp print keyobserver" << getFileName() << '\n';
    }*/
};